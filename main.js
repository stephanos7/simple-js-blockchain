// stephanosCoin

class Block {

  // index [optional] = position of block in the chain
  // timestamp        = time stamp against the creation of the block
  // data             = payload values 
  // previous Hash    = String of the previous block's hash value

  constructor(index, timestamp, data, previousHash = ""){

  }


}